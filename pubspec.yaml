name: flutter_template
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.2.1
  zeronet_ws:
    git: https://github.com/ZeroNetX/zeronet_ws.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0